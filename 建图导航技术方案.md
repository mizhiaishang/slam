# 建图导航技术方案

## 1. 目标

当前方案将“建图”和“导航”拆成两条独立链路：

- 建图链路负责把批量语义 JSON 构造成可索引、可可视化、可查询的语义导航图。
- 导航链路负责在已有图上，基于“当前位置 + 目标物体”输出一条 waypoint 路径和对应坐标。

这样做的核心目的有两个：

- 避免实时导航阶段重复做建图计算。
- 让图构建、图导出、路径规划可以分别维护和替换。

## 2. 当前保留的核心文件

- `build_nav_graph_nx.py`
  当前主图模型与主算法文件。负责图结构定义、JSON 导入、节点与边更新、空间查询、目标物体导航规划。
- `nav_graph_export_utils.py`
  图内容导出与可视化导出工具。负责 JSON 内容导出、二维地图导出、三维交互视图导出。
- `process_semantic_graphs_batch.py`
  面向 `C:\Users\mizhiaishang\Desktop\semantic_graphs` 的批处理入口。负责按 `step0 -> stepN` 顺序建图并生成导出结果。
- `object_navigation_runtime.py`
  独立导航运行脚本。负责加载已生成图文件，输入当前位置，输出路径点信息。
- `nav_graph_config.json`
  建图参数配置文件。

## 3. 图结构设计

### 3.1 节点层级

当前图结构固定为三层：

- `topology`
  表示高层区域、语义簇或拓扑片区。
- `waypoint`
  表示一帧输入或一个途径点，是导航的主通行层。
- `object`
  表示语义物体节点，挂载在 waypoint 下。

层级关系为：

`topology -> waypoint -> object`

### 3.2 边类型

当前保留三类有效边：

- `contains_waypoint`
  `topology -> waypoint`
  语义：该 waypoint 属于哪个 topology。
- `temporal`
  `waypoint -> waypoint`
  语义：相邻途径点可连通，这条边也是导航真正使用的通行边。
- `mounts_object`
  `waypoint -> object`
  语义：该 waypoint 观测到了该 object。

说明：

- 当前不在主图中持久化 `topology -> topology` 边。
- `topology -> topology` 连接只在导出阶段根据 `waypoint -> waypoint` 跨 topology 相邻关系推导，用于展示和分析，不参与真实路径规划。

### 3.3 节点关键属性

#### topology 节点

- `topology_id`
- `topology_key`
- `centroid`
- `waypoint_count`
- `object_count`
- `first_seen`
- `last_seen`

#### waypoint 节点

- `waypoint_id`
- `topology_id`
- `topology_key`
- `timestamp`
- `sort_key`
- `position`
- `rotation`
- `source_path`
- `observations`
- `observation_count`

#### object 节点

- `object_id`
- `topology_id`
- `class_name`
- `centroid`
- `observation_count`
- `waypoint_count`
- `mean_confidence`
- `mean_depth`
- `nav_candidate`
- `is_dynamic`
- `sample_image_path`

## 4. 建图输入约定

默认输入为一批 JSON 文件，每个 JSON 视为一个 waypoint。

最小输入语义如下：

```json
{
  "timestamp": "20260401",
  "position": [0.0, 0.0, 0.0],
  "rotation": [1.0, 0.0, 0.0, 0.0],
  "class_node": "node_0",
  "detections": [
    {
      "class_name": "chair",
      "confidence": 0.92,
      "global_position": [1.0, 2.0, 0.0],
      "depth": 1.8,
      "bbox": {
        "x1": 10,
        "y1": 20,
        "x2": 30,
        "y2": 40
      },
      "test_img_file": "frame_0001.jpg"
    }
  ]
}
```

字段意义：

- `position` 决定 waypoint 的空间位置。
- `class_node` 决定该 waypoint 属于哪个 topology。
- `detections` 决定 waypoint 下挂载哪些 object。
- `global_position` 决定检测是否能被结构化成 object 节点。

## 5. 建图算法流程

### 5.1 文件发现与排序

`process_semantic_graphs_batch.py` 会扫描 `step*/info.json`，并按数字步序而不是字符串顺序导入：

- `step0`
- `step1`
- `step2`
- ...

这样可以保证 waypoint 链和真实采样顺序一致。

### 5.2 topology 创建与更新

每个 payload 会先根据 `topology_key_field` 取出拓扑键。

如果该 topology 不存在：

- 新建 topology 节点
- 初始化 centroid、时间范围和计数

如果已存在：

- 用新的 waypoint 位置更新 centroid
- 更新 `waypoint_count`
- 更新 `first_seen / last_seen`

### 5.3 waypoint 创建与相邻连边

每个 payload 会生成一个 waypoint 节点，然后：

- 通过 `contains_waypoint` 边挂到所属 topology
- 与前一个 waypoint 建立 `temporal` 边

`temporal` 边是当前导航的唯一可通行边。

### 5.4 object 创建与去重

每条 detection 会优先检查：

- `class_name` 是否有效
- `confidence` 是否满足阈值
- 是否存在 `global_position`

如果没有 `global_position`：

- 不创建 object 节点
- 只保留原始 observation

如果有 `global_position`：

- 在同一个 topology 内查找“同名且近邻”的已有 object
- 若找到则合并到已有 object
- 若找不到则创建新 object

当前去重规则：

- 去重范围只在同一个 topology 内
- 同类名
- 距离小于 `merge_distance`

这样可以避免跨区域错误合并。

### 5.5 原始观测保留

每条 detection 不论是否成功建 object，都会保存在 waypoint 的 `observations` 中。

这样做的价值：

- 便于后续回溯
- 便于重新跑 object 聚类逻辑
- 便于调试误匹配

## 6. 导出与可视化方案

### 6.1 结构化导出

`nav_graph_export_utils.py` 会导出：

- `topologies`
- `waypoints`
- `objects`
- `waypoint_sequence`
- `edges.topology_waypoint`
- `edges.waypoint_waypoint`
- `edges.waypoint_object`
- `edges.topology_topology_inferred`

其中 `topology_topology_inferred` 由 waypoint 邻接关系推导，主要用于展示。

### 6.2 二维可视化

二维视图按真实 `x/y` 坐标绘制：

- 蓝线：waypoint 路径
- 彩色 topology 区域和中心点
- 绿色 object：可导航物体
- 红色 object：动态物体

### 6.3 三维可视化

三维视图按真实 `x/y/z` 展示 waypoint 与 object。

当前特殊处理：

- topology 节点不使用真实高度
- topology 节点统一抬升到单独显示层
- 保留 `topology -> waypoint` 连线
- 根据 waypoint 相邻关系推导 topology 之间的可视化连接

## 7. 导航方案

### 7.1 输入

导航脚本只依赖两类输入：

- 当前机器人位置 `current_position = [x, y, z]`
- 目标物体查询 `object_query`

### 7.2 路径规划流程

`build_nav_graph_nx.py` 中的 `plan_navigation_to_object(...)` 采用如下逻辑：

1. 用 `current_position` 找到最近 waypoint。
2. 用 `object_query` 匹配 object。
3. 找到每个 object 直接挂载的 waypoint。
4. 从主图中抽取 waypoint-only 的可通行子图。
5. 仅在 `temporal + traversable=True` 边上做最短路径。
6. 对多个目标候选 waypoint 选取最短可达路径。
7. 输出最终路径点信息与坐标信息。

### 7.3 导航输出

导航运行脚本 `object_navigation_runtime.py` 输出：

- `current_waypoint`
- `selected_target`
- `path_points`
- `path_coordinates`

其中：

- `path_points` 适合控制层逐点导航
- `path_coordinates` 适合直接送到运动或轨迹模块

## 8. 为什么采用这套方案

### 8.1 建图和导航解耦

建图阶段负责重处理、聚类、导出。

导航阶段只加载图并规划路径，不重复做建图计算。

### 8.2 waypoint 作为真实通行层

拓扑节点用于组织与解释，不直接参与通行。

真实可达关系由 waypoint 邻接边决定，这更符合“走点导航”的执行模式。

### 8.3 object 只挂在 waypoint 下

这样可以从“目标物体”直接回溯到“可到达的观测点”，路径规划更直接。

## 9. 当前局限

- object 查询当前是基于 `object_id / class_name / 子串` 的简单匹配。
- 路径规划当前使用最短路，没有做方向代价、转弯代价、障碍重规划。
- `object_navigation_runtime.py` 目前用脚本常量指定目标图路径和默认目标物体。
- 三维可视化依赖 Plotly CDN，离线环境下浏览器无法自动加载 CDN。

## 10. 后续建议

- 把导航输入改成标准函数接口或服务接口。
- 给 waypoint 边增加朝向代价、风险代价或通行状态。
- 给 object 增加白名单和导航优先级。
- 给导航脚本增加“下一目标 waypoint”增量输出接口。
- 如果后续有实时建图需求，可在保留本方案的前提下补充在线 append 流程。
