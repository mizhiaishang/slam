# 建图导航交接文档

## 1. 当前交付范围

本次交付只保留当前有效主线：

- NetworkX 建图
- 图内容导出
- 二维/三维可视化
- 基于目标物体的 waypoint 导航

已移除的冗余内容：

- 旧 SQLite 建图脚本
- 旧 SQLite 说明文档
- 一次性导航验证脚本
- 临时定位脚本

## 2. 当前工作区保留文件

- `build_nav_graph_nx.py`
  核心图模型与算法。
- `nav_graph_export_utils.py`
  图导出与可视化导出。
- `process_semantic_graphs_batch.py`
  桌面 `semantic_graphs` 数据集批处理入口。
- `object_navigation_runtime.py`
  独立导航脚本。
- `nav_graph_config.json`
  建图参数配置。

## 3. 典型运行方式

### 3.1 批量建图

当前面向桌面 `semantic_graphs` 数据集，直接运行：

```bash
py process_semantic_graphs_batch.py
```

输出目录：

`C:\Users\mizhiaishang\Desktop\semantic_graphs\generated_outputs`

主要输出文件：

- `semantic_graphs_nav_graph.pkl`
- `semantic_graphs_nav_graph_contents.json`
- `semantic_graphs_nav_graph_stats.json`
- `semantic_graphs_nav_graph_visualization.html`
- `semantic_graphs_nav_graph_visualization.svg`
- `semantic_graphs_nav_graph_visualization_3d.html`

### 3.2 独立导航

输入当前位置：

```bash
py object_navigation_runtime.py -4.3965,-0.4874,0.2397
```

输入当前位置并临时指定目标物体：

```bash
py object_navigation_runtime.py -4.3965,-0.4874,0.2397 chair
```

输出文件：

`C:\Users\mizhiaishang\Desktop\semantic_graphs\generated_outputs\semantic_graphs_nav_graph_runtime_path_points.json`

## 4. 当前脚本职责划分

### `build_nav_graph_nx.py`

职责：

- 图结构定义
- JSON 导入
- topology / waypoint / object 创建与更新
- waypoint 邻接边维护
- object 去重
- 原始观测保存
- 邻近查询
- 导航路径规划

不负责：

- 批量扫描特定数据集目录
- 可视化导出
- 导航结果文件落盘格式整理

### `nav_graph_export_utils.py`

职责：

- 导出结构化 JSON
- 导出二维坐标图
- 导出三维交互图
- 推导拓扑可视化连接

不负责：

- 建图主逻辑
- 导航规划

### `process_semantic_graphs_batch.py`

职责：

- 批量扫描 `semantic_graphs`
- 按 `step0 -> stepN` 顺序导入
- 统一生成建图结果与可视化结果

适用场景：

- 数据集离线重建
- 可视化验证
- 导出分析材料

### `object_navigation_runtime.py`

职责：

- 读取现成图
- 输入当前位置
- 查目标物体
- 返回路径点与坐标

适用场景：

- 独立导航调用
- 上层控制程序集成
- 离线验证

## 5. 关键配置说明

配置文件：

- `nav_graph_config.json`

当前主要影响建图的字段：

- `topology_key_field`
- `merge_distance`
- `distance_dims`
- `waypoint_cell_size`
- `object_cell_size`
- `topology_cell_size`
- `min_confidence`
- `extra_dynamic_classes`

说明：

- `process_semantic_graphs_batch.py` 会读取这份配置，但会固定自己的输入输出目录。
- `object_navigation_runtime.py` 当前不走这份配置，而是用脚本常量指定图路径和默认目标物体。

## 6. 已完成验证

本次交付前已完成：

- `py -m py_compile build_nav_graph_nx.py`
- `py -m py_compile nav_graph_export_utils.py`
- `py -m py_compile process_semantic_graphs_batch.py`
- `py -m py_compile object_navigation_runtime.py`
- `py process_semantic_graphs_batch.py`
- `py object_navigation_runtime.py -4.396506060195695,-0.4874186573698919,0.2397014703558273`

验证结果：

- 建图成功
- 二维可视化成功
- 三维可视化成功
- 目标物体导航成功返回路径点与坐标

## 7. 当前输出格式说明

### 建图输出

`semantic_graphs_nav_graph_contents.json` 适合做：

- 结构检查
- 节点关系分析
- 可视化输入
- 人工排查

### 导航输出

`semantic_graphs_nav_graph_runtime_path_points.json` 适合做：

- 下发路径点
- 逐点导航
- 上层任务管理读取

建议优先使用这两个字段：

- `path_points`
- `path_coordinates`

## 8. 当前风险与注意事项

- 物体匹配仍然是简单名称匹配，复杂语义检索还未接入。
- waypoint 路径基于已导入顺序，若源数据顺序错误，会直接影响导航结果。
- object 去重只在 topology 内执行，如果 topology 划分不合理，会影响 object 聚类质量。
- 三维 HTML 依赖 Plotly CDN，离线浏览器环境下会失去交互渲染能力。

## 9. 推荐后续接手顺序

建议新接手同学按这个顺序理解系统：

1. 先看 `build_nav_graph_nx.py`，理解图结构和路径规划。
2. 再看 `process_semantic_graphs_batch.py`，理解离线建图入口。
3. 再看 `nav_graph_export_utils.py`，理解导出与可视化。
4. 最后看 `object_navigation_runtime.py`，理解导航运行入口。

## 10. 建议的下一步演进

- 把 `object_navigation_runtime.py` 封装成纯函数接口或服务接口。
- 增加目标物体优先级和多目标选择策略。
- 增加 waypoint 可达状态更新能力。
- 增加导航执行阶段的“下一跳 waypoint”输出格式。
- 如需在线系统集成，可把当前脚本改造成 API 服务，而不是继续扩展命令行脚本。
